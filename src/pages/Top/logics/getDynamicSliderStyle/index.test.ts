import { generateSlideStyle } from '.';

describe('getDynamicSliderStyle', () => {
  test('スライドが4枚で左から2番目が大きい画像のとき、正しいスタイルが返る', () => {});
  test('offsetがスライド枚数を超えるとエラーがでる', () => {});
  test('テスト', () => {});
});
